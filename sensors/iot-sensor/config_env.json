{
    "mqtt": {
        "host": "${MQTT_BROKER_HOST}",
        "port": ${MQTT_BROKER_PORT},
        "topic": "${MQTT_BROKER_TOPIC}"
    },
    
    "sensors": {
        "Sensor 1": {
            "header":{"id": "sensor1","version": "v1.6"}
        },
        "Sensor 2": {
            "header":{"id": "sensor2","version": "v1.6"}
        }
    }
}
